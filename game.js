window.onload = function() {
    var config = {
        width: 800,
        height: 600,
        backgroundColor: '#87CEEB',
        scene: [FirstScene]
    }
    var HuntTheDuck = new Phaser.Game(config);
}
